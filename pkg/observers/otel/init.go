package otel

// init registers the OTEL observer
// For now, we're creating a standalone observer without orchestrator integration
func init() {
	// Observer can be created directly using NewObserver()
	// No orchestrator registration needed for now
}
